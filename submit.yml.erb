---
batch_connect:
  template: "basic"
script:
  native:
    - "-N"
    - "1"
    - "-n"
    - "<%= num_cores %>"
    <%- if node_type != "any" -%>
    - "-C"
    - "<%= node_type %>"
    <%- end -%>
